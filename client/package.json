{
  "name": "webpad-client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host=0.0.0.0",
    "build": "vite build",
    "b": "rm -rf ../server/dist && npm run build && mv dist ../server",
    "preview": "vite preview --host=0.0.0.0"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  },
  "dependencies": {
    "@use-gesture/vanilla": "^10.3.1"
  }
}
